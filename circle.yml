machine:
  services:
    - docker
    - mysql

dependencies:
  override:
    - docker info
    - docker build -t phalcon:0.0.1 .
# コメント追加
test:
  override:
    - docker run -it phalcon:0.0.1 php /var/www/sample1/vendor/phpunit/phpunit/phpunit -c /var/www/sample1/tests/
