machine:
  services:
    - docker
    - mysql

dependencies:
  override:
    - docker info
    - docker build -t phalcon:0.0.1 .
# コメント追加
test:
  override:
    - docker run -it -v `pwd`/src:/var/www/ phalcon:0.0.1 phpunit -c /var/www/sample1/tests/
